document.addEventListener('DOMContentLoaded',()=>{
    var body = document.querySelector('body');
    function fired(){
        console.log('the function fired try again next time harder !!');
    }
    var 
    function hoverd(){
        console.log('hoverd ');
    }
    logo.addEventListener('click',fired);
    logo.addEventListener('onclick',hoverd)
})
